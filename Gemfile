source 'https://rubygems.org'

# something broke in the 0.12.1 update (github actions fails to install a native dependency somehow), 
# which I have no idea how to fix, so let's try to fix the version at 0.11.5 for now...
# https://rubygems.org/gems/cddl/versions/0.12.1
gem 'regexp_property_values', '1.5.1'
gem 'cddl', '0.11.5'
